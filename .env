# Environment variables for AI Lead Message Generator
# Copy this file to .env and fill in your values.
# These are OPTIONAL — all values have sensible defaults.

# Override AI provider base URLs (only needed for proxies/custom deployments)

VITE_GEMINI_BASE_URL=https://generativelanguage.googleapis.com
VITE_GROQ_BASE_URL=https://api.groq.com/openai/v1
